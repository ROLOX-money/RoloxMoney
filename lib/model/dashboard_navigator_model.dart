class DashboardNavigatorModel {
  String? icon;
  String? icon1;
  String? name;
  int? index;
  bool? isSelected;

  DashboardNavigatorModel(
      {this.name, this.isSelected = false, this.icon, this.index, this.icon1});
}
